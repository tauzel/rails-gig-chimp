<div class="container">
  <div class="row justify-content-center">
    <div class="col md-6">
    <%#  <div class="booking-form">
        <div class="form-group"> %>
          <img height="150" width="150" class="avatar-large" <%= image_tag @student.picture_url %>
          <h2><%= @student.name %></h2>
          <p class="text-secondary"><%= @student.batch %></p>
          <p class="text-secondary"><%= @student.description %></p>
        </div>
      <%#  <div class="col md-4 order-md-2">
          <h3 class="d-flex justify-content-between align-items-center mb-3">
            <span class="text-muted">Book this student</span>
          </h3>
          <h4 class="mb-3">Price 100€ per hour</h4>
          <div class="row">
          </div>
          <div class="form-btn">
            <button class="btn-ghost">Request booking</button>
          </div>

        </div>
      </div>
    </div>
  </div>
</div> %>
